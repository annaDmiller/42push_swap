#include "push_swap.h"

int min_of_two(int num1, int num2)
{
    if (num1 < num2)
        return (num1);
    else
        return (num2);
}

int max_of_two(int num1, int num2)
{
    if (num1 > num2)
        return (num1);
    else
        return (num2);
}